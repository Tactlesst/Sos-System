import '../styles/globals.css'; // Import global styles if needed
import 'leaflet/dist/leaflet.css';
export default function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />; // Default behavior
}